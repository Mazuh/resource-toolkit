import makeReduxAssets from '../src';

test('true is true', () => {
  expect(true).toBe(true);
});

describe('reducer factory', () => {
  it('determine loading state for read', () => {
    // todo
  });
});
